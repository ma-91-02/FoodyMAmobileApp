class selectService {
  constructor(id, lang, message, byApp, byWaiter, pageTitle) {
    this.id = id;
    this.lang = lang;
    this.message = message;
    this.byApp = byApp;
    this.byWaiter = byWaiter;
    this.pageTitle= pageTitle;
  }
}

export default selectService;
