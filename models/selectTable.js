class SelectTable {
  constructor(id, lang, title, button, pageTitle) {
    this.id = id;
    this.lang = lang;
    this.title = title;
    this.button = button;
    this.pageTitle = pageTitle;
  }
}

export default SelectTable;
